{
  "name": "skillforge-monorepo",
  "private": true,
  "workspaces": ["packages/*"],
  "scripts": {
    "build": "npm run build -w packages/core && npm run build -w packages/mcp && npm run build -w packages/cli",
    "test": "npm run test -w packages/core && npm run test -w packages/mcp && npm run test -w packages/cli",
    "lint": "eslint packages/*/src/**/*.ts"
  },
  "devDependencies": {
    "typescript": "^5.5.0",
    "@types/node": "^20.0.0",
    "vitest": "^2.0.0"
  }
}
